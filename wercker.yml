box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "0.18.1"

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
        repo: novakgroupoxford/bicycle-hugo
